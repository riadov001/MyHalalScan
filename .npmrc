auto-install-peers=false
strict-peer-dependencies=false
# Required for EAS Build CI — pnpm 10.x sets frozen-lockfile=true in CI by default
# which breaks monorepo builds where the lockfile was generated on a slightly
# different pnpm patch version. Set to false so EAS can install normally.
frozen-lockfile=false
